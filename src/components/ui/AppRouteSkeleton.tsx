"use client";

/** @deprecated Prefer `AppRouteLoadingSkeleton` in `loading.tsx`; kept for any legacy imports. */
export { AppRouteLoadingSkeleton as AppRouteSkeleton } from "@/components/route-loading/skeletons";
