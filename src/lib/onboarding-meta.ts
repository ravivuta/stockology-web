/**
 * Stored on the Supabase Auth user via `updateUser({ data })` — tied to the account, not this browser.
 * Web also skips onboarding when `user_portfolio_snapshots` shows existing data (mobile sync).
 */
export const STOCKS_PM_ONBOARDING_USER_META_KEY = "stocks_pm_onboarding_done";
