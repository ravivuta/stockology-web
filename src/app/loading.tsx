/**
 * Home (`/`) is the marketing landing — avoid a fake “skeleton landing” while RSC streams.
 * A brief empty paint is preferable to the wrong layout.
 */
export default function RootLoading() {
  return null;
}
